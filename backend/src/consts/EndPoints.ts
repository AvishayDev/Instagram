



export enum EndPoints {
    LIKES='likes',
    POSTS='posts',
    USERS='users'
}


export enum UsersEndPoints {
    CHECK='check',
    REGISTER='register',
    LOGIN='login'
}

export enum PostsEndPoints {
    ADD='add',  
}

export enum LikesEndPoints {
    SIGN='sign',
    UNSIGN='unsign'
}